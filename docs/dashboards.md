# AI/BI Dashboard (Optional)

- Create a dashboard, add datasets: `airports_silver_mv`, `lookupcodes_silver_mv`, `flights_silver_mv`, `airports_by_city_mv`.
- Try NL-to-viz prompts like “Count of Flight Delays”.
- Add a date filter bound to `flights_silver_mv.FlightDate`.
- Build a parameterized dataset for `dest = :parameter` and bind a Single Value filter.
